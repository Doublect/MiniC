#ifndef AST_H
#define AST_H
    enum class VariableType {
        INT = -2,
        FLOAT = -4,
        BOOL = -5,
    };

    enum TypeSpecType {
        INT = -2,
        VOID = -3,
        FLOAT = -4,
        BOOL = -5,
    };
#endif